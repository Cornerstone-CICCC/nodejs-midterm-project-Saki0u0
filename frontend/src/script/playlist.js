document.addEventListener('DOMContentLoaded', () => { 
  
  const addBtn = document.querySelector(".add-btn") 

  addBtn.addEventListener('click',() => {
    const playlist = document.createElement('div');
    playlist.style.border = "solid 1px black"

  })
})
